# seoul-district-air-mcp — 개발 지침 (CLAUDE.md)

## 목적
"지금 우리 동네 공기 어때?"에 즉시 답하는 단일 목적 MCP.
데이터셋: 서울 열린데이터광장 "서울시 실시간 자치구별 대기환경 현황" (ListAirQualityByDistrictService) 하나만 다룬다.

※ 참고: `hlucent/seoul-air-quality-mcp`는 담당자 개인용으로 여러 데이터셋을 조합한 별도 저장소이며,
이 프로젝트(`seoul-district-air-mcp`)와 무관함. 혼동 주의.

## 절대 원칙
1. 이 데이터셋 하나만 다룬다. 다른 대기 데이터셋(경보 현황, 도로변 측정 등)은 여기 끼워 넣지 않는다. 필요하면 별도 MCP 후보로만 기록.
2. 대상은 시민. 원본 코드값(MSRSTN_PBADMS_CD 등) 노출 금지, 자치구명으로 입출력.
3. 단순하고 예측 가능한 구조 우선. 화려한 기능보다 몇 년 뒤에도 이해 가능한 코드.
4. 기존 "서울시 대기환경정보 MCP"(17개 도구)는 참고용 프로토타입일 뿐, 이번 산출물 아님.

## API 스펙
- 서비스명: ListAirQualityByDistrictService
- TYPE: json 권장
- 파라미터: KEY, TYPE, SERVICE, START_INDEX, END_INDEX, MSRSTN_PBADMS_CD(선택)
- 1회 최대 1,000건 (25개 구라 문제 없음)
- 일일 호출 제한: 문서 미명시 → rate limit 에러 발생 시 처리 필요 (에러코드 참고)
- 매시간 갱신
- 자치구 코드 매핑표는 코드에 상수로 고정 (25개 구, 원본 xls 참고)
- 출력 필드: MSRMT_YMD, MSRSTN_PBADMS_CD, MSRSTN_NM, CAI, CAI_GRD, CRST_SBSTN, NTDX(NO2), OZON(O3), CBMX(CO), SPDX(SO2), PM(PM10), FPM(PM2.5)

## 도구 설계 (1개만)

### get_district_air_quality
- 입력: `district` (string, 선택). "강남", "강남구", "gangnam" 등 느슨한 매칭 허용, 25개 구 리스트 안에서만 매칭. 매칭 실패 시 "정확한 자치구명을 확인해주세요" 안내.
- 생략 시: 25개 구 전체 반환 + 최고/최악 구 요약 추가
- 반환: 자치구, 측정시각, CAI, CAI등급, 지수결정물질, PM10(+등급), PM2.5(+등급), 오존(+등급), 이산화질소(+등급), 일산화탄소(+등급), 아황산가스(+등급)

### 개별 항목 등급 표기 규칙 (중요)
- CAI_GRD는 API가 이미 제공 → 그대로 사용.
- PM10/PM2.5/O3/NO2/CO/SO2 개별 등급은 API가 안 줌 → 아래 우선순위로 처리:
  1. **먼저 확인**: 연결된 MCP 중 통합대기환경지수/오염물질 등급 기준을 제공하는 데이터셋(MCP)이 있으면 그걸 우선 사용해서 등급 계산.
  2. 없으면: 환경부 통합대기환경지수(CAI) 항목별 등급 기준표를 코드에 상수로 박아두고 계산 (좋음/보통/나쁨/매우나쁨 4단계).
  - 어느 방식을 썼는지 응답에 출처 주석 정도는 남겨도 됨(사용자 노출 X, 로그/코드 주석 수준).

## 에러 처리
- API 에러코드(ERROR-3xx/5xx/6xx)는 공통 핸들러로 묶어 "일시적 오류, 잠시 후 재시도"로 단순화
- 자치구명 미매칭은 위 안내 문구로 처리

## 인증
- 인증키는 발급 완료, 환경변수로 주입 (.env, 저장소에 커밋 금지)

## 배포
- GitHub 저장소: `hlucent/seoul-district-air-mcp` (이 MCP 전용, 단일 저장소 원칙)

## Rate limit 미들웨어 (2026-08-16 지침 변경)
공개 무인증 서버(`/mcp`)에 클라이언트별 요청 폭주를 막기 위한 3단계 방어. IP 기준, 메모리 내 상태로 관리.
1. 분당(60초 슬라이딩 윈도우) 3회 초과 → 429
2. 1시간 내 429 응답을 5회 이상 받은 IP는 이후 24시간 차단(모든 요청 429)
3. IP당 24시간 rolling 총 호출 30회 초과 → 429

임계값(3/5/30)은 고정값. 실사용 트래픽 관찰 후 조정 필요 시 다시 "지침 변경"으로 명시 요청.

## 확장 후보 (지금 안 함 — TODO만)
- CAI 등급 기반 알림/구독 기능 → 별도 MCP
- 시계열/추이 데이터 → 다른 데이터셋 필요, 범위 밖
- 캐싱(1시간 단위 가능) → rate limit 문제 실제 발생 시 재검토

## 문서화
- README.md, 개발일지는 AI가 직접 작성, 개발일지는 진행 중 주요 결정/이슈 그때그때 기록
- **완료된 작업은 즉시 DEVLOG.md에 기록**: 기능 추가, 버그 수정, 재배포 등 작업 단위가 하나 끝날 때마다(세션이 끝나기를 기다리지 않고) 날짜와 함께 DEVLOG.md에 바로 남긴다. 무엇을 왜 바꿨는지, PR/커밋 링크가 있다면 함께 남긴다.
- 이 CLAUDE.md와 이미 확정된 사항은 세션마다 재논의하지 않음. 변경 필요 시 "지침 변경"으로 명시 요청.
